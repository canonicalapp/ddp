/**
 * Schema Generator
 * Generates comprehensive schema.sql files for tables, columns, constraints, and indexes
 *
 * This is a re-export of the refactored SchemaGenerator for backward compatibility
 */

export { SchemaGenerator } from './schema';
